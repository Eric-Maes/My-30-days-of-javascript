/**
 * Create a _variable.js_ file and declare variables with string, boolean, undefined and null data types.
 */
let varString = "I'm a string";
let varBool = true;
let varUndefined;
let varNull = null;

/**
 * Use the typeof JavaScript operator to check the different data types of each variable.
 * We can use console.log to display types in the console.
 */
typeof varString;
typeof varBool;
typeof varUndefined;
typeof varNull;

/**
 * Declare four variables without assigning values.
 */
let undefined_1;
let undefined_2;
let undefined_3;
let undefined_4;

/**
 * Declare four variables with assigned values.
 */
let defined_1 = "I'm a string";
let defined_2 = 22;
let defined_3 = false;
let defined_4 = 10.59;

/**
 * Declare variables to store your _first name_ and _last name_ on several lines.
 */
let firstName = "Eric";
let lastName = "Maës";

/**
 * Declare variables to store your _marital status_, _country_ and _age_ on a single line.
 */
let is_married = false, country = "France", age = 22;

/**
 * Declare two variables _myAge_ and _yourAge_ with initial values and display the following in the console.
 */
let myAge = 22;
let yourAge = 20;
console.log("I am", myAge, "years old.\nYou are", yourAge, "years old.");